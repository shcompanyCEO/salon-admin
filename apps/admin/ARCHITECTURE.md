# Salon Admin - 프로젝트 아키텍처

## 📐 시스템 아키텍처

```
┌─────────────────────────────────────────────────────────────┐
│                      Client (Browser)                        │
│  ┌───────────────────────────────────────────────────────┐  │
│  │           Next.js 14 (App Router)                     │  │
│  │  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐  │  │
│  │  │   Pages     │  │ Components  │  │   Stores    │  │  │
│  │  │  (Routes)   │  │  (UI/Biz)   │  │  (Zustand)  │  │  │
│  │  └─────────────┘  └─────────────┘  └─────────────┘  │  │
│  │         │                │                 │          │  │
│  │         └────────────────┴─────────────────┘          │  │
│  │                          │                             │  │
│  │                    API Client                          │  │
│  └──────────────────────────┼──────────────────────────┘  │
└────────────────────────────┼─────────────────────────────┘
                              │
                         HTTP/HTTPS
                              │
┌────────────────────────────┼─────────────────────────────┐
│                      Backend API                          │
│  ┌──────────────────────────────────────────────────────┐ │
│  │              REST API Server                         │ │
│  │  ┌──────────┐  ┌──────────┐  ┌──────────┐          │ │
│  │  │   Auth   │  │ Business │  │   Data   │          │ │
│  │  │  Layer   │  │  Logic   │  │  Access  │          │ │
│  │  └──────────┘  └──────────┘  └──────────┘          │ │
│  └──────────────────────┼───────────────────────────────┘ │
└────────────────────────┼─────────────────────────────────┘
                         │
┌────────────────────────┼─────────────────────────────────┐
│                    Database                               │
│  ┌──────────────────────────────────────────────────────┐ │
│  │  PostgreSQL / MySQL / MongoDB                        │ │
│  │  ┌──────┐ ┌──────┐ ┌──────┐ ┌──────┐               │ │
│  │  │Users │ │Salons│ │Books │ │Reviews│              │ │
│  │  └──────┘ └──────┘ └──────┘ └──────┘               │ │
│  └──────────────────────────────────────────────────────┘ │
└───────────────────────────────────────────────────────────┘
```

## 🗂️ 디렉토리 구조 상세

```
salon-admin/
│
├── src/
│   │
│   ├── app/                          # Next.js App Router
│   │   ├── layout.tsx               # Root layout
│   │   ├── page.tsx                 # Home page (redirect)
│   │   ├── globals.css              # Global styles
│   │   │
│   │   ├── login/                   # Authentication
│   │   │   └── page.tsx
│   │   │
│   │   ├── dashboard/               # Dashboard
│   │   │   └── page.tsx
│   │   │
│   │   ├── bookings/                # 예약 관리
│   │   │   ├── page.tsx            # 예약 목록
│   │   │   └── [id]/               # 예약 상세
│   │   │       └── page.tsx
│   │   │
│   │   ├── customers/               # 고객 관리
│   │   │   ├── page.tsx
│   │   │   └── [id]/
│   │   │       └── page.tsx
│   │   │
│   │   ├── staff//               # 직원 관리
│   │   │   ├── page.tsx
│   │   │   └── [id]/
│   │   │       └── page.tsx
│   │   │
│   │   ├── services/                # 서비스 관리
│   │   │   └── page.tsx
│   │   │
│   │   ├── reviews/                 # 리뷰 관리
│   │   │   └── page.tsx
│   │   │
│   │   ├── sales/                   # 매출 관리
│   │   │   └── page.tsx
│   │   │
│   │   ├── chat/                    # 채팅
│   │   │   ├── page.tsx
│   │   │   └── [roomId]/
│   │   │       └── page.tsx
│   │   │
│   │   ├── salons/                  # 살롱 관리 (슈퍼 관리자)
│   │   │   └── page.tsx
│   │   │
│   │   ├── users/                   # 사용자 관리
│   │   │   └── page.tsx
│   │   │
│   │   └── settings/                # 설정
│   │       └── page.tsx
│   │
│   ├── components/
│   │   │
│   │   ├── ui/                      # 재사용 가능한 UI 컴포넌트
│   │   │   ├── Button.tsx
│   │   │   ├── Input.tsx
│   │   │   ├── Select.tsx
│   │   │   ├── Modal.tsx
│   │   │   ├── Card.tsx
│   │   │   ├── Table.tsx
│   │   │   └── Badge.tsx
│   │   │
│   │   ├── layout/                  # 레이아웃 컴포넌트
│   │   │   ├── Layout.tsx
│   │   │   ├── Sidebar.tsx
│   │   │   └── Header.tsx
│   │   │
│   │   └── features/                # 기능별 컴포넌트
│   │       ├── bookings/
│   │       ├── customers/
│   │       └── staff/
│   │
│   ├── store/                       # Zustand 상태 관리
│   │   ├── authStore.ts            # 인증 상태
│   │   ├── salonStore.ts           # 살롱 관련 상태
│   │   ├── bookingStore.ts         # 예약 상태
│   │   └── uiStore.ts              # UI 상태 (사이드바, 모달 등)
│   │
│   ├── lib/                         # 유틸리티 및 헬퍼
│   │   ├── api.ts                  # API 클라이언트
│   │   └── utils.ts                # 유틸리티 함수
│   │
│   ├── locales/                     # 다국어 지원
│   │   ├── ko.ts                   # 한국어
│   │   ├── en.ts                   # 영어
│   │   ├── th.ts                   # 태국어
│   │   └── useTranslation.ts       # 번역 훅
│   │
│   └── types/                       # TypeScript 타입 정의
│       └── index.ts                # 전체 타입 정의
│
├── public/                          # 정적 파일
│   ├── images/
│   └── icons/
│
├── .env.example                     # 환경 변수 예시
├── .gitignore                       # Git ignore 설정
├── next.config.js                   # Next.js 설정
├── tailwind.config.js              # Tailwind CSS 설정
├── tsconfig.json                    # TypeScript 설정
├── package.json                     # 의존성 관리
└── README.md                        # 프로젝트 문서
```

## 🔄 데이터 흐름

```
┌────────────┐
│   User     │
│  Action    │
└──────┬─────┘
       │
       ▼
┌────────────────┐
│     Page       │ ──► 사용자 인터페이스
│  Component     │     (React Component)
└───────┬────────┘
        │
        ▼
┌────────────────┐
│  Store/State   │ ──► 상태 관리
│   (Zustand)    │     (Global State)
└───────┬────────┘
        │
        ▼
┌────────────────┐
│  API Client    │ ──► HTTP 요청
│  (fetch/axios) │     (REST API Call)
└───────┬────────┘
        │
        ▼
┌────────────────┐
│  Backend API   │ ──► 비즈니스 로직
│                │     (Server Processing)
└───────┬────────┘
        │
        ▼
┌────────────────┐
│   Database     │ ──► 데이터 저장
│                │     (Persistence)
└────────────────┘
```

## 🔐 인증 흐름

```
1. Login Request
   ┌─────────┐
   │ User    │
   │ Enters  │
   │ Creds   │
   └────┬────┘
        │
        ▼
   ┌─────────────┐
   │ Login Page  │
   └──────┬──────┘
          │
          ▼
   ┌──────────────┐
   │  authApi     │ ──► POST /api/auth/login
   │  .login()    │
   └──────┬───────┘
          │
          ▼
   ┌──────────────┐
   │  Backend     │
   │  Validates   │
   └──────┬───────┘
          │
          ▼
   ┌──────────────┐
   │  Returns     │
   │  JWT Token   │
   └──────┬───────┘
          │
          ▼
   ┌──────────────┐
   │  authStore   │
   │  .login()    │ ──► Store user & token
   └──────┬───────┘
          │
          ▼
   ┌──────────────┐
   │  Redirect    │
   │  Dashboard   │
   └──────────────┘

2. Authenticated Requests
   ┌──────────────┐
   │  API Call    │
   └──────┬───────┘
          │
          ▼
   ┌──────────────┐
   │  Get Token   │ ──► From authStore
   │  from Store  │
   └──────┬───────┘
          │
          ▼
   ┌──────────────┐
   │  Add Bearer  │ ──► Authorization: Bearer {token}
   │  Header      │
   └──────┬───────┘
          │
          ▼
   ┌──────────────┐
   │  Send to     │
   │  Backend     │
   └──────────────┘
```

## 📊 상태 관리 구조

```
Zustand Stores
│
├── authStore
│   ├── user: User | null
│   ├── token: string | null
│   ├── isAuthenticated: boolean
│   ├── login(user, token)
│   ├── logout()
│   └── updateUser(data)
│
├── salonStore
│   ├── currentSalon: Salon | null
│   ├── staff: Staff[]
│   ├── services: Service[]
│   ├── setCurrentSalon(salon)
│   ├── addStaff(staff)
│   ├── updateStaff(id, data)
│   └── addService(service)
│
├── bookingStore
│   ├── bookings: Booking[]
│   ├── selectedDate: Date
│   ├── addBooking(booking)
│   ├── updateBooking(id, data)
│   ├── cancelBooking(id)
│   └── getBookingsByDate(date)
│
└── uiStore
    ├── isSidebarOpen: boolean
    ├── locale: Locale
    ├── isModalOpen: boolean
    ├── toggleSidebar()
    ├── setLocale(locale)
    └── openModal(content)
```

## 🎯 컴포넌트 계층 구조

```
App
│
├── Layout
│   ├── Sidebar
│   │   └── Navigation Items
│   │       ├── Dashboard Link
│   │       ├── Bookings Link
│   │       ├── Customers Link
│   │       └── ...
│   │
│   ├── Header
│   │   ├── Menu Toggle
│   │   ├── Language Selector
│   │   ├── Notifications
│   │   └── Logout Button
│   │
│   └── Main Content
│       │
│       ├── Dashboard Page
│       │   ├── Stats Cards
│       │   ├── Booking Status
│       │   ├── Recent Bookings
│       │   └── Top Designers
│       │
│       ├── Bookings Page
│       │   ├── Filters
│       │   ├── Table
│       │   └── New Booking Modal
│       │
│       ├── Customers Page
│       │   ├── Search
│       │   ├── Stats
│       │   ├── Table
│       │   └── New Customer Modal
│       │
│       └── Employee Page
│           ├── Stats
│           ├── Employee Cards
│           └── New Employee Modal
│
└── Common UI Components
    ├── Button
    ├── Input
    ├── Select
    ├── Modal
    ├── Card
    ├── Table
    └── Badge
```

## 🔌 API 엔드포인트 구조

```
API Base URL: /api

Authentication
├── POST   /auth/login
├── POST   /auth/logout
├── POST   /auth/register
└── POST   /auth/forgot-password

Salons
├── GET    /salons
├── GET    /salons/:id
├── POST   /salons
├── PUT    /salons/:id
└── DELETE /salons/:id

Employees
├── GET    /salons/:salonId/employees
├── GET    /employees/:id
├── POST   /salons/:salonId/employees
├── PUT    /employees/:id
└── DELETE /employees/:id

Services
├── GET    /salons/:salonId/services
├── GET    /services/:id
├── POST   /salons/:salonId/services
├── PUT    /services/:id
└── DELETE /services/:id

Bookings
├── GET    /bookings
├── GET    /bookings/:id
├── POST   /bookings
├── PUT    /bookings/:id
├── POST   /bookings/:id/cancel
└── POST   /bookings/:id/complete

Customers
├── GET    /salons/:salonId/customers
├── GET    /customers/:id
├── POST   /salons/:salonId/customers
├── PUT    /customers/:id
└── DELETE /customers/:id

Reviews
├── GET    /reviews
├── GET    /reviews/:id
└── POST   /reviews/:id/respond

Sales
├── GET    /salons/:salonId/sales
└── GET    /employees/:employeeId/sales

Chat
├── GET    /chat/rooms
├── GET    /chat/rooms/:roomId
├── GET    /chat/rooms/:roomId/messages
├── POST   /chat/rooms/:roomId/messages
└── POST   /chat/rooms/:roomId/read

Upload
├── POST   /upload/image
└── POST   /upload/images
```

## 🔒 권한 관리

```
Role-Based Access Control (RBAC)

SUPER_ADMIN
├── All salon management
├── Approve/reject salons
├── View all statistics
└── System monitoring

MANAGER (Salon Owner/Manager)
├── Manage own salon
├── Manage staff & schedule
├── Manage services
├── View all bookings
├── Manage customers
├── View sales reports
└── Manage users

STAFF (Designer/Intern)
├── View own schedule
├── Manage own bookings
├── View customer info
├── Respond to reviews
└── View own sales

CUSTOMER
├── Make bookings
├── View own bookings
├── Write reviews
└── Chat with salon
```
