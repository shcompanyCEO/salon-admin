# 🚀 Salon Admin - 빠른 시작 가이드

## 📦 설치 및 실행 (5분 안에!)

### 1️⃣ 의존성 설치

```bash
cd salon-admin
npm install
```

### 2️⃣ 환경 변수 설정

```bash
# .env.local 파일 생성
cp .env.example .env.local

# .env.local 파일 내용 (필요시 수정)
NEXT_PUBLIC_API_URL=http://localhost:3001/api
```

### 3️⃣ 개발 서버 실행

```bash
npm run dev
```

브라우저에서 http://localhost:3000 접속!

### 4️⃣ 데모 로그인

```
이메일: admin@salon.com
비밀번호: password123
```

---

## 📱 주요 페이지 미리보기

### 🏠 대시보드 (`/dashboard`)
- 실시간 통계 카드 (오늘 예약, 매출, 고객 수)
- 예약 상태 현황 (대기/완료/취소)
- 최근 예약 목록
- TOP 디자이너 순위

### 📅 예약 관리 (`/bookings`)
- 날짜/상태/디자이너별 필터링
- 예약 목록 테이블
- 새 예약 추가 모달
- 예약 상세 정보 및 수정

### 👥 고객 관리 (`/customers`)
- 고객 검색 (이름/전화번호/고객번호)
- 고객 통계 (총 고객, 단골 고객, 최근 방문)
- 고객 목록 및 히스토리
- 자동 고객번호 생성

### ✂️ 디자이너 관리 (`/designers`)
- 디자이너 카드 그리드 뷰
- 평점 및 리뷰 수 표시
- 전문 분야 태그
- 권한 설정 및 활성화 관리

---

## 🎨 구현된 기능 체크리스트

### ✅ 인증 및 권한
- [x] 로그인 페이지
- [x] JWT 토큰 기반 인증
- [x] 역할별 접근 제어 (RBAC)
- [x] 로그아웃 기능

### ✅ UI/UX
- [x] 반응형 디자인 (모바일/태블릿/데스크톱)
- [x] 다크 모드 준비 (Tailwind CSS)
- [x] 사이드바 네비게이션
- [x] 다국어 지원 (한/영/태)

### ✅ 핵심 컴포넌트
- [x] Button, Input, Select
- [x] Modal, Card, Badge
- [x] Table (정렬, 페이지네이션 준비)
- [x] Layout (Header, Sidebar)

### ✅ 상태 관리
- [x] Zustand 스토어 (Auth, Salon, Booking, UI)
- [x] LocalStorage 영속성
- [x] 타입 안전성 (TypeScript)

### ✅ 페이지
- [x] 로그인
- [x] 대시보드
- [x] 예약 관리
- [x] 고객 관리
- [x] 디자이너 관리

### 🚧 개발 예정
- [ ] 서비스 관리 페이지
- [ ] 리뷰 관리 페이지
- [ ] 매출 분석 페이지
- [ ] 실시간 채팅
- [ ] 설정 페이지
- [ ] 이미지 업로드
- [ ] 데이터 내보내기 (CSV/Excel)
- [ ] 푸시 알림

---

## 🔧 개발 팁

### 새로운 페이지 추가하기

```bash
# 1. 페이지 파일 생성
src/app/new-page/page.tsx

# 2. 라우트는 자동으로 생성됨!
# /new-page 로 접근 가능
```

### 새로운 API 엔드포인트 추가하기

```typescript
// src/lib/api.ts
export const newApi = {
  getData: () => apiClient.get('/new-endpoint'),
  createData: (data: any) => apiClient.post('/new-endpoint', data),
};
```

### 새로운 Zustand 스토어 추가하기

```typescript
// src/store/newStore.ts
import { create } from 'zustand';

interface NewState {
  data: any[];
  setData: (data: any[]) => void;
}

export const useNewStore = create<NewState>((set) => ({
  data: [],
  setData: (data) => set({ data }),
}));
```

### 다국어 번역 추가하기

```typescript
// src/locales/ko.ts
export const ko = {
  // ... 기존 번역
  newSection: {
    title: '새로운 섹션',
    button: '클릭',
  },
};
```

---

## 📚 유용한 명령어

```bash
# 개발 서버 실행
npm run dev

# 프로덕션 빌드
npm run build

# 프로덕션 서버 실행
npm run start

# 린트 검사
npm run lint

# 타입 체크
npx tsc --noEmit
```

---

## 🐛 트러블슈팅

### 문제: 포트 3000이 이미 사용 중

```bash
# 다른 포트로 실행
PORT=3001 npm run dev
```

### 문제: 모듈을 찾을 수 없음

```bash
# node_modules 삭제 후 재설치
rm -rf node_modules
npm install
```

### 문제: TypeScript 오류

```bash
# TypeScript 캐시 삭제
rm -rf .next
npm run dev
```

---

## 🎯 다음 단계

1. **백엔드 API 연동**
   - API 서버 구축
   - 데이터베이스 스키마 설계
   - API 엔드포인트 구현

2. **추가 페이지 개발**
   - 서비스 관리
   - 리뷰 관리
   - 매출 분석
   - 채팅

3. **고급 기능 추가**
   - 실시간 업데이트 (WebSocket)
   - 푸시 알림 (Firebase)
   - 이미지 업로드 (AWS S3)
   - PDF 리포트 생성

4. **테스트 작성**
   - 단위 테스트 (Jest)
   - 통합 테스트
   - E2E 테스트 (Playwright)

5. **배포**
   - Vercel 배포
   - 환경 변수 설정
   - 도메인 연결

---

## 💡 참고 자료

- [Next.js 문서](https://nextjs.org/docs)
- [Tailwind CSS 문서](https://tailwindcss.com/docs)
- [Zustand 문서](https://docs.pmnd.rs/zustand)
- [TypeScript 문서](https://www.typescriptlang.org/docs)

---

**Happy Coding! 🎉**
